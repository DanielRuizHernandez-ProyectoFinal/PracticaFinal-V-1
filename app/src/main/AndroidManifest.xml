<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.iessochoa.danielruizhernandez.practicafinal_v_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticaFinalV1">

        <activity android:name=".LogIn.EditProfileActivity"
            android:exported="true"/>

        <activity
            android:name=".LogIn.ActivityPrincipal"
            android:exported="true"
            android:label="DnD Administrador"/>
        <activity
            android:name=".LogIn.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".LogIn.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>